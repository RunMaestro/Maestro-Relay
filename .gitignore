node_modules/
dist/
.env
*.db
logs/

# plugin packaging output (staged copy + signed .tgz) and any signing keys
plugin-dist/
*.pem
*.key

Auto Run Docs/
models
.maestro/

# generated Teams app package (built by `npm run deploy-teams`)
appPackage/maestro-relay-teams.zip

# local PR-review worktree (not part of the project)
.pr52-review/
